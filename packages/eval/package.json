{
  "name": "@apatureai/verdict-eval",
  "version": "0.1.4",
  "private": true,
  "license": "MIT",
  "type": "module",
  "description": "Eval harness: synthetic canaries, golden-set metrics, regression + quality gate, model/prompt registry.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "vitest run",
    "gen:calibration-contract": "tsc -b && node scripts/gen-calibration-contract.ts",
    "build": "tsc -b"
  },
  "dependencies": {
    "@apatureai/verdict-db": "workspace:*",
    "@apatureai/verdict-types": "workspace:*"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.4"
  }
}
