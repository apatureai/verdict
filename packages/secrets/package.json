{
  "name": "@engine/secrets",
  "version": "0.1.3",
  "private": true,
  "license": "MIT",
  "type": "module",
  "description": "Engine encryption: per-tenant CMK + per-repo data-key envelope helper (no per-repo KMS keys).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "files": ["dist"],
  "scripts": { "test": "vitest run" }
}
