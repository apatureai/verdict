{
  "name": "@apatureai/verdict-jobs",
  "version": "0.1.4",
  "private": true,
  "license": "MIT",
  "type": "module",
  "description": "Engine async job store: idempotent enqueue, pg_notify dispatch, SKIP LOCKED claim over Postgres.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "files": ["dist"],
  "scripts": { "test": "vitest run" },
  "dependencies": {
    "@apatureai/verdict-db": "workspace:*"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.4"
  }
}
