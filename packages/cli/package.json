{
  "name": "@apatureai/verdict-cli",
  "version": "0.1.4",
  "private": true,
  "license": "MIT",
  "type": "module",
  "description": "judgment-engine CLI: run a grounded design review against a URL or the bundled demo site.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "bin": { "judgment-engine": "./dist/main.js" },
  "files": ["dist", "fixtures"],
  "scripts": { "test": "vitest run" },
  "dependencies": {
    "@apatureai/verdict-capture": "workspace:*",
    "@apatureai/verdict-context": "workspace:*",
    "@apatureai/verdict-critique": "workspace:*",
    "@apatureai/verdict-review": "workspace:*",
    "@apatureai/verdict-storage": "workspace:*",
    "@apatureai/verdict-types": "workspace:*"
  }
}
