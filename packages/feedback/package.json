{
  "name": "@apatureai/verdict-feedback",
  "version": "0.1.3",
  "private": true,
  "license": "MIT",
  "type": "module",
  "description": "Engine feedback: explicit/implicit signal capture, per-repo memory, rater down-weighting, preference export.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "vitest run",
    "contract:gen": "node scripts/preference-contract.ts"
  },
  "dependencies": {
    "@apatureai/verdict-db": "workspace:*",
    "@apatureai/verdict-storage": "workspace:*",
    "@apatureai/verdict-types": "workspace:*"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.4",
    "fast-check": "^4.9.0"
  }
}
